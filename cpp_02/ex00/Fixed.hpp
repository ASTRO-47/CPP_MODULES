#ifndef FIXED_HPP
#define FIXED_HPP\


#include <iostream>

class test {
public:
        int a;
        char *buffer;
        int b;
    test()
    {
        // nothinh to do for now
    };
    test(test &obj)
    {
        // a = obj.a;
        // b = obj.b;
        // nothing to do
    };
};

#endif
