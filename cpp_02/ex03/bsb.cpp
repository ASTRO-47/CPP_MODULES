#include "Point.hpp"

// the shoelace formula https://en.wikipedia.org/wiki/Shoelace_formula

// Area=∣x1​(y2​−y3​)+x2​(y3​−y1​)+x3​(y1​−y2​)∣ / 2

Fixed   cla_area(const Point &a, const Point &b, const Point &c)
{
    return (Fixed(a.get_x() )) //working on the return value to calculate the area of each triangle
}

bool bsp( Point const a, Point const b, Point const c, Point const point)
{

    return (true);
}