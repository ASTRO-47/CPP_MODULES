#include "Array.hpp"

// using namespace std; For function templates, the compiler automatically
// deduces the type from the arguments provided.



int main()
{
    int * a = new int();
    std::cout << *a << std::endl;
    return 0;
}
